You are an expert full-stack SaaS architect. Build a complete, production-ready web application called **Tabio** — an exact functional replica of PetPooja restaurant POS & management software, but with completely different branding.

Tagline: "Tabio – Intelligent POS for Restaurants: Seamless Orders, Smart Inventory, Unlimited Growth"
Branding: Modern blue + green theme, minimalist, premium feel. Use Tailwind + shadcn/ui. All images replaced with modern stock photos. Logo: clean "Tabio" text.

Tech Stack (Replit friendly):
- Next.js 15 (App Router) + React + TypeScript
- Tailwind + shadcn/ui
- Prisma + PostgreSQL (Supabase or Replit DB)
- Socket.io for real-time
- NextAuth for auth
- PDF generation (for bills/KOTs)

Requirement: The ENTIRE internal logic must be 100% perfect and fully functional out-of-the-box (no placeholders for core features). Handle every edge case (split bills, variants, add-ons, concurrent orders, stock conversions, etc.).

Build ALL PetPooja modules exactly:

1. Multi-tenant outlets + roles (Owner, Manager, Cashier, Kitchen, Waiter)
2. Real-time Dashboard + live sales + active orders + low-stock alerts
3. Menu Management (items, variants, add-ons, availability, multiple menus)
4. POS & Billing (3-click flow, table management, split/merge, discounts, custom bills with QR/logo)
5. KOT System (station-wise routing, simulate printing via PDF + browser print)
6. Order Management (dine-in, takeaway, delivery + aggregator orders)
7. Inventory (auto-deduction on order, raw material conversion, low-stock, reports)
8. Reports & Analytics (80+ reports with charts)
9. CRM + Loyalty points
10. Settings (taxes, printers simulation, users)

Aggregator Integrations (Zomato & Swiggy):
- Implement FULLY WORKING MOCK versions that behave exactly like the real APIs.
- Create dedicated folders: /app/api/integrations/zomato/ and /app/api/integrations/swiggy/
- Include realistic endpoints:
  - Menu push/pull (add/update/get menu)
  - Order webhooks (new order, status update, confirm/reject)
  - Status sync (accept, preparing, ready, dispatched)
  - Payout simulation
- Use fake data + polling + Socket.io to show live orders coming in.
- Use environment variables (ZOMATO_API_KEY, SWIGGY_API_KEY, etc.).
- Add clear // TODO comments everywhere: "// Replace this mock with real Zomato API call here"
- Simulate the exact flow restaurants see in PetPooja.

Novel Features (fully working):
1. AI Demand Forecasting (7-day prediction)
2. Voice POS (Web Speech API)
3. WhatsApp Order Mock
4. Carbon Footprint Tracker + green suggestions
5. AI Menu Optimizer
6. Advanced Franchise Dashboard

Security, mobile-friendly POS UI, PDF bills/KOTs, printer simulation — all perfect.

After building:
- Deploy the app automatically
- Create .env.example with all required keys
- Generate a complete MANUAL_INTEGRATION_GUIDE.md file that lists exactly what a developer must do to replace mocks with LIVE Zomato, Swiggy, Razorpay, and real thermal printers (include official links and step-by-step code changes)
- Include README.md with test instructions for every module
- Make code clean, commented, modular, and ready for production export

Start building the full app now. Create project structure first, then implement module by module if needed, but deliver a complete working MVP. Show progress and only ask for clarification if absolutely necessary.

Begin.